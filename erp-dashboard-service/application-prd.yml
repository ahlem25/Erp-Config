server:
  port: 8065

spring:
  application:
    name: erp-dashboard-service
  datasource:
    url: ${DB_URL:jdbc:mysql://prd-db.erp.com:3306/dashboard_db?useSSL=true&serverTimezone=UTC}
    username: ${DB_USERNAME:dashboard_prd}
    password: ${DB_PASSWORD}
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  instance:
    prefer-ip-address: true
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URL:http://prd-eureka.erp.com:8013/eureka/}